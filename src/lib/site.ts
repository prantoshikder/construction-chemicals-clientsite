export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://mountbd.org";

export const SITE_NAME = "MCCEL";

export const ROUTES = [
  "/",
  "/about",
  "/services",
  "/products",
  "/gallery",
  "/blogs",
  "/contact",
] as const;
